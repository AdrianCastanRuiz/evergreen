import { EmptyState } from "@/components/ui/empty-state";

// Family Menu tab (FR10, UX-DR13). Placeholder established by Story 1.10;
// the weekly menu with day tabs lands in Epic 6 (Story 6.2).
export default function MenuTabScreen() {
  return <EmptyState title="Menu" body="This week's menu will appear here soon." />;
}